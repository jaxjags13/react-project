import { ErrorResponse } from '@remix-run/router';
import {Card as BCard} from 'react-bootstrap';

function Card(props) {
    const smallStyle = {
        color: 'red'
    }

    return (
        <>
        for(let i )
            <small style={smallStyle}>{errors[i]}</small>
        </>
    );
}

export default Card;